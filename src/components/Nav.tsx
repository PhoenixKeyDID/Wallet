/**
 * Host-contract: top navigation.
 *
 * Placeholder so the `/wallet` and `/night` example pages compile standalone.
 * The PhoenixKey host app supplies the real `@/components/Nav` — point the alias
 * at it when integrating.
 */
export function Nav() {
  return <nav aria-label="Phoenix" className="w-full" />;
}
