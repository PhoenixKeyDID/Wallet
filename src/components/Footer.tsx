/**
 * Host-contract: page footer.
 *
 * Placeholder so the `/wallet` and `/night` example pages compile standalone.
 * The PhoenixKey host app supplies the real `@/components/Footer` — point the
 * alias at it when integrating.
 */
export function Footer() {
  return <footer className="w-full" />;
}
